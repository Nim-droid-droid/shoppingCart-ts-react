// Create a card per character
  // in each card have a link 'browse all toys' that takes them to a page that list out all that chars toys

// apply all ur react knowldg in this prj

import {useState} from "react"

export function Cards(){
  const [state, setState] = useState()

  return(
    <></>
  )
}