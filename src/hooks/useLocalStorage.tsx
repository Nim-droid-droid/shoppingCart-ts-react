// Custom Hook
// Even if user refreshes the page, items stay in the cart

// make this custom hook work with custom generic types & different props


export function useLocalStorage(){
  return(
    <>
    </>
  )
}
